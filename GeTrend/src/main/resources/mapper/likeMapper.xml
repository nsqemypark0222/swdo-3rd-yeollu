<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yeollu.getrend.store.dao.LikeMapper">

<insert id="likeInsert" parameterType="like">
insert into 
	likes (
		store_no
		, user_email
	) values(
		#{store_no}
		,#{user_email}
	)
</insert>

<delete id="likeDelete">
delete from
	likes
where
	store_no = #{store_no}
and
	user_email = #{user_email}
</delete>

<select id="likeSelectByEmail" parameterType="string" resultType="like">
select
	store_no
	,user_email
from
	likes
where
	user_email = #{user_email}	
</select>

<select id="likeSelectByStoreno" parameterType="string" resultType="like">
select
	store_no
	,user_email
from
	likes
where
	store_no = #{store_no}	
</select>

</mapper>

