<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yeollu.getrend.store.dao.InstaLocationInfoMapper">
<insert id="insertInstaLocationInfo" parameterType="instaLocationInfo">
insert into 
	insta_locations_info (
		location_info_no
		, store_no
		, location_id
		, location_x
		, location_y
	) values(
		seq_insta_locations_info.nextval
		,#{store_no}
		, #{location_id}
		, #{location_x}
		, #{location_y}
	)
</insert>

</mapper>

