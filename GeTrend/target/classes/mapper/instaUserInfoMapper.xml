<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yeollu.getrend.store.dao.InstaUserInfoMapper">
<insert id="insertInstaUserInfo" parameterType="instaUserInfo">
insert into 
	insta_users_info (
		user_info_no
		, store_no
		, profile_pic_url
		, insta_id
	) values(
		seq_insta_users_info.nextval
		, #{store_no}
		, #{profile_pic_url}
		, #{insta_id}
	)
</insert>

</mapper>

