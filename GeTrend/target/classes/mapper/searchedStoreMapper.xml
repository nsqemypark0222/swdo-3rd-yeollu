<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yeollu.getrend.store.dao.SearchedStoreMapper">

<insert id="insertSearchedStore" parameterType="searchedStore">
insert into 
	searched_stores (
		searched_no
		, store_name
	) values(
		seq_searched_stores.nextval
		, #{store_name}
	)
</insert>

<select id="isExistedSearchedStore" parameterType="string" resultType="string">
select
	decode(count(*), 0, 'false', 'true') as result
from
	searched_stores
where
	store_name = #{store_name}
	and to_char(searched_indate, 'HH24') = to_char(sysdate, 'HH24')
</select>

</mapper>

